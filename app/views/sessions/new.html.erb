<!DOCTYPE html>
<html lang="pt-BR" class="h-full bg-gray-50">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login | SegFox</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-full flex items-center justify-center">
  <div class="w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-6">
    <!-- Logo -->
    <div class="flex justify-center">
      <%= image_tag "banner.png", alt: "SegFox Logo", class: "h-16 w-auto object-contain" %>
    </div>
    <h2 class="text-center text-2xl font-extrabold text-gray-900">Faça seu login</h2>
    <!-- Login Form -->
    <%= form_with url: login_path, method: :post, local: true, class: "mt-6 space-y-4" do |form| %>
      <div>
        <%= form.label :email, "Email", class: "block text-sm font-medium text-gray-700" %>
        <%= form.email_field :email, required: true, autofocus: true, class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-green-500" %>
      </div>
      <div>
        <%= form.label :password, "Senha", class: "block text-sm font-medium text-gray-700" %>
        <%= form.password_field :password, required: true, class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-green-500" %>
      </div>
      <div class="flex items-center justify-between">
        <div class="flex items-center">
          <%= form.check_box :remember_me, class: "h-4 w-4 text-green-600 focus:ring-green-500 border-gray-300 rounded" %>
          <%= form.label :remember_me, "Lembrar-me", class: "ml-2 block text-sm text-gray-900" %>
        </div>
        <div class="text-sm">
          <%= link_to "Esqueceu a senha?", "#", class: "font-medium text-green-600 hover:text-green-500" %>
        </div>
      </div>
      <div>
        <%= form.submit "Entrar", class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500" %>
      </div>
    <% end %>
  </div>
</body>
</html>
